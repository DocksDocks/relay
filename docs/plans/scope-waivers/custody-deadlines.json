{
  "schema": 1,
  "waivers": [
    {
      "file": "Cargo.toml",
      "path": "src/tests/workspace_lease_process.rs",
      "reason": "Names this path only as the `path` of an existing `[[test]]` target. Step load_regression adds a `#[test] fn` inside that target and neither renames the file nor adds a target, so `autotests = false` still resolves it and Cargo.toml needs no edit. Verified: the target is already declared, so `cargo test --test workspace_lease_process` selects the new function with no manifest change."
    },
    {
      "file": "scripts/gate.mjs",
      "path": "test/rust-test-inventory.mjs",
      "reason": "Invokes this harness as a tooling entry point with fixed arguments. Steps custody_forgiveness and runtime_exchange_retry change only the `UNIT_TEST_FLOOR` constant inside the harness, not its argument surface, its exit contract, or its PASS line format, so the caller is unaffected. Fixing the gate's own skip-detection hole is a declared non-goal recorded in Environment."
    },
    {
      "file": "test/reentry-inventory.mjs",
      "path": "test/fixtures/reentry-inventory.json",
      "reason": "This is the generator that owns the fixture, so it necessarily names it. The plan regenerates the fixture through `node test/reentry-inventory.mjs --generate` and never edits the generator; A24 proves generator and fixture agree afterwards. The genuine coupling P21 found in this family - the fixture being moved by a new `poll()` syscall site - was fixed by declaring the fixture rather than waived."
    }
  ]
}
