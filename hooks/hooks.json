{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/relay",
            "args": ["hook"],
            "statusMessage": "session-relay: registering + draining inbox"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/relay",
            "args": ["hook", "--event", "prompt"],
            "statusMessage": "session-relay: checking inbox"
          }
        ]
      }
    ]
  }
}
