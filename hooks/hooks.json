{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/relay",
            "args": ["hook"],
            "statusMessage": "session-relay: registering + draining inbox"
          }
        ]
      }
    ]
  }
}
